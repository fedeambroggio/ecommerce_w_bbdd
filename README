Initiate mongodb on localhost:27017 to use login, chat and table of products functions
Authentication and session info is handled passport and will last 10 minutes per user and one extra added every time user performs a request.
Run `server.js` to initiate server
Navigate to `localhost:8080/register` to sign up
Navigate to `localhost:8080/login` to login
After login you will be redirected to `localhost:8080/dashboard` and be authorized to use app
